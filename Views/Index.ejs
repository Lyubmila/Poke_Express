<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle%></title>
</head>
<body>
    
    <h1><%= pageHeader %></h1>

    <a href="/pokemon/new">Create New Pokemon</a>

    <% pokemonData.map( (pokemon, index) => { %>
        <li>
            Pokemon name: <%= pokemon.name[0].toUpperCase() + pokemon.name.substring(1) %> <br>

            <!-- <%= pokemon.name.charAt(0).toUpperCase() + pokemon.name.slice(1)%> -->


            <img src =  <%=` ${pokemon.img}.jpg` %>  width ="400" height="300" > <br>

            <!-- <a href=<%=`/pokemon/${index}`%>> Click Me! </a> -->

            <a href='/pokemon/<%= pokemon._id %>' >Details</a>


        </li>
    <% } ) %>
</body>
</html>